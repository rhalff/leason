#
# Test the primitives
#
---
  options:
    captureFormat:
      minCount: 1
  data:
    one: "test@example.com"

  schema:
    '$schema': 'http://json-schema.org/draft-04/schema#'
    type: "object"
    properties:
      one:
        type: "string"
        format: "email"
