#
# There is only one type here so the format will still be:
#
#  items: { type: "number" }
---
data:
  four: [0,22,23]

schema:
  '$schema': 'http://json-schema.org/draft-04/schema#'
  type: "object"
  properties:
    four:
      type: "array"
      items:
        type: "number"
